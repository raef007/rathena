//-----------
// drops.conf
//-----------

// The rate the common items are dropped (Items that are in the ETC tab, besides card)
item_rate_common: 100
item_rate_common_boss: 100
item_rate_common_mvp: 100

// The rate healing items are dropped (items that restore HP or SP)
item_rate_heal_boss: 100
item_rate_heal_mvp: 100

// The rate at which usable items (in the item tab) other then healing items are dropped.
item_rate_use_boss: 100
item_rate_use_mvp: 100

// The rate at which equipment is dropped.
item_rate_equip: 100
item_rate_equip_boss: 100
item_rate_equip_mvp: 100

// The rate at which cards are dropped
item_rate_card: 100
item_rate_card_boss: 100
item_rate_card_mvp: 100

// The rate adjustment for the MVP items that the MVP gets directly in their inventory
// Mode: 0 - official order, 1 - random order, 2 - all items
item_rate_mvp: 100

// The rate adjustment for equip-granted item drops.
item_rate_adddrop: 100

// The rate adjustment for items inside of equip-granted item group drops.
// This is used by Ore Discovery and items such as Gaia Sword, Jewel Sword, Blazzer Card, Tengu Card and Dokkaebi Horn.
item_group_rate: 100

//-----------
// exp.conf
//-----------

// Rate at which exp. is given. (Note 2)
base_exp_rate: 300

// Rate at which job exp. is given. (Note 2)
job_exp_rate: 300

//-----------
// feature.conf
//-----------
// Roulette (Note 1)
// Requires: 2014-10-22bRagexe or later
feature.roulette: off

// Roulette bonus reward
// Multiply amount by 2 if the reward item ID is the same as bonus item ID
feature.roulette_bonus_reward: off

//-----------
// autobattle.conf
//-----------

// Default auto-battle time per character in seconds (0 = unlimited, 10800 = 3 hours)
autobattle_default_seconds: 0

// VIP bonus seconds granted on login (3 hours = 10800, VIP total = 6 hours)
autobattle_vip_bonus_seconds: 10800

// Maximum possible auto-battle time including VIP bonuses (24 hours = 86400)
autobattle_max_seconds: 86400

// Milliseconds between roam attempts when no enemies in range (default 3000 = 3 seconds)
autobattle_roam_interval: 3000

// EXP penalty % during auto-battle (0 = no penalty, 50 = half exp, 100 = no exp)
// Base and Job EXP are independent. Loaded from DB autobattle_settings if rows exist.
autobattle_exp_penalty_base: 0
autobattle_exp_penalty_job: 0

//-----------
// Classic feel tuning (Renewal mode with Pre-RE gameplay)
//-----------

// Traps always visible (Pre-RE behavior; Renewal default hides traps)
traps_setting: 0

// Floras/Marine Spheres behave as Pre-RE (can be damaged outside PvP)
alchemist_summon_setting: 15

// Natural HP/SP recovery stops at 50% weight (Pre-RE value; Renewal default: 70%)
natural_heal_weight_rate: 50

// Item boxes blocked at 70% weight (Pre-RE value; Renewal default: 90%)
open_box_weight_rate: 70

// ASPD cap: all job tiers locked to Pre-RE max of 190 (RENEWAL_ASPD is disabled,
// but these caps ensure 3rd/extended stat entries don't override if ever re-enabled)
max_third_aspd: 190
max_extended_aspd: 190
max_summoner_aspd: 190

// Stat caps: lock 3rd/extended stat caps to 99 (Pre-RE limit) in case they are
// referenced before a 3rd-job NPC is re-enabled in future
max_third_parameter: 99
max_third_trans_parameter: 99
max_baby_third_parameter: 80
max_extended_parameter: 99
