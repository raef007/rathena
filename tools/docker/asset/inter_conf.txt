// =================================================================
// Docker local-dev override for conf/import/inter_conf.txt
// All five DB hostnames point at the `db` docker-compose service
// (mariadb container). Credentials come from conf/inter_athena.conf
// which already matches the compose env vars: ragnarok/ragnarok/ragnarok.
// =================================================================

login_server_ip: db
ipban_db_ip: db
char_server_ip: db
map_server_ip: db
log_db_ip: db
